import SwiftUI

/// Dates/Termine tab placeholder (P0-06). The urgency-grouped agenda with
/// countdowns and reminders is built in P3-07/08.
struct DatesView: View {
    var body: some View {
        PlaceholderScreen(titleKey: "tab_dates", systemImage: "calendar")
    }
}
