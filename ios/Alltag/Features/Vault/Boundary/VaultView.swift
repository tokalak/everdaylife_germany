import SwiftUI

/// Docs/Vault tab placeholder (P0-06). The document list, scan/import, and
/// on-device-only trust banner are built in P3-05.
struct VaultView: View {
    var body: some View {
        PlaceholderScreen(titleKey: "tab_docs", systemImage: "folder")
    }
}
